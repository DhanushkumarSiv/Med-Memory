# MedMemory OS runtime requirements

# Core runtime
Node.js>=18
npm>=9

# Database (choose one)
Neo4j AuraDB (recommended)
# OR local Neo4j 5.x (Docker optional)

# Optional for local Neo4j setup
Docker Desktop (optional)

# Required environment variables (.env)
ANTHROPIC_API_KEY
NEO4J_URI
NEO4J_USER
NEO4J_PASSWORD
NEO4J_DATABASE
JWT_SECRET
JWT_EXPIRES_IN
OTP_EXPIRY_MINUTES
PORT
NODE_ENV

# Install project dependencies
# npm install
