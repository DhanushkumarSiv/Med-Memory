# MedMemory OS runtime requirements (non-Python project)

# This repository does not use Python packages.
# It runs on Node.js + Neo4j.

# Core runtime
node>=18
npm>=9

# Database
neo4j>=5

# Optional for local Neo4j setup
docker-desktop

# Required environment variables (.env in code/server)
ANTHROPIC_API_KEY
NEO4J_URI
NEO4J_USER
NEO4J_PASSWORD
NEO4J_DATABASE
JWT_SECRET
JWT_EXPIRES_IN
OTP_EXPIRY_MINUTES
PORT
NODE_ENV

# Install dependencies
# cd code/server && npm install
# cd ../client && npm install
