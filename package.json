{
  "name": "medmemory-os",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "code/client",
    "code/server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace code/server\" \"npm run dev --workspace code/client\"",
    "dev:all": "docker compose up --build",
    "dev:stack": "start.bat",
    "dev:server": "npm run dev --workspace code/server",
    "dev:client": "npm run dev --workspace code/client",
    "build": "npm run build --workspace code/server && npm run build --workspace code/client",
    "seed": "npm run seed --workspace code/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
